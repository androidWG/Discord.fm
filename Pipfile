[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
pypresence = ">=4.2.1"
pylast = ">=5.0.0"
pystray = ">=0.19.4"
future = ">=0.18.2"
pillow = ">=9.2.0"
psutil = ">=5.9.1"
requests = ">=2.28.1"
plyer = ">=2.0.0"
filelock = ">=3.7.1"
pywin32 = {version = ">=306", markers = "sys_platform == 'windows'"}
aquaui = {version = ">=0.0.1.post2", markers = "sys_platform == 'darwin'"}
pygobject = {version = ">=3.44.1", markers = "sys_platform == 'linux'"}

[dev-packages]
packaging = ">=21.3"
black = "*"
pytest = "*"
PyYAML = {version = ">=6.0", markers = "sys_platform == 'linux'"}
requirements-parser = {version = ">=0.5.0", markers = "sys_platform == 'linux'"}
PyInstaller = {version = ">=5.10.1", markers = "sys_platform == 'linux'"}

[requires]
python_version = "3.11"
